// ── Role-based access control ─────────────────────────────────────────
// Stub — enforcement will be added once roles are finalized.
// Replace the no-op body with real logic when ready.

export function requireRole(
  _role: string,
  _allowed: readonly string[],
  _action: string
): void {
  // TODO: enforce role checks once roles are defined
}
